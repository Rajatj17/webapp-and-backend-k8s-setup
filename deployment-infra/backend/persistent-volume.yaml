apiVersion: v1
kind: PersistentVolume
metadata:
  name: persistent-volume
  namespace: kube-test
  labels:
    name: pv-vol1
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: '1Gi'
  hostPath:
    path: /home/rajat/data

  # awsElasticBlockStore:
      #   volumeId: some-id
      #   fsType: ext4