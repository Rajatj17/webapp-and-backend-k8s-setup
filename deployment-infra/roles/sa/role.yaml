apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: helm-test
  name: helm-test-role
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "deployments"]
    verbs: ["list", 'get', 'delete', 'describe', 'delete', 'update']
